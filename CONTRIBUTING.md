# Contributing

Keep changes safe by default. Any new probe must document authorization assumptions, request volume, timeout behavior, collected data, false positives and an offline test. Do not add exploit payloads, credential attacks, stealth features, rate-limit bypasses or automatic scope expansion.
